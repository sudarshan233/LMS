import NavBar from "../Components/NavBar.jsx";

const StudentPage = () => {
    return (
        <div>
            <NavBar />
            {/*{navClicked === "Courses" ? <Courses /> : <Stats />}*/}
        </div>
    );
}

export default StudentPage;